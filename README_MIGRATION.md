# ACEOV GitBook Migration

This copy is prepared for GitHub/GitBook Git Sync.

- Markdown pages, images, and PDFs are stored locally.
- Videos are referenced as remote GitBook file embeds to keep the GitHub repository small.
- Navigation is defined in `SUMMARY.md`; GitBook config is in `.gitbook.yaml`.
